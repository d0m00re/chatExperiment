import React, { ReactElement } from 'react'

interface Props {
    
}

function CreateRoom({}: Props): ReactElement {
    return (
        <div>
            go create a room
        </div>
    )
}

export default CreateRoom
