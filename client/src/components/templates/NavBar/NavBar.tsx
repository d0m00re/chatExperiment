import React, { ReactElement } from 'react'

interface Props {
    
}

function NavBar({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default NavBar
